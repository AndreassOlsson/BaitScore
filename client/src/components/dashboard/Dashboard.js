import React from 'react';
import PropTypes from 'prop-types';

const dashboard = (props) => {
  return <div className='whole-screen'>Dashboard</div>;
};

dashboard.propTypes = {};

export default dashboard;
